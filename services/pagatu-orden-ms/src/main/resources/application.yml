spring:
  application:
    name: pagatu-orden-ms
  profiles:
    active: dev
  cloud:
    config:
      name: orden-ms
  config:
    import: "optional:configserver:${CONFIG_SERVER_URL:http://localhost:18888}"
